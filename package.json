{
  "name": "bit-final",
  "version": "1.0.0",
  "description": "soc. tinklas lietuviams",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --parallel client:* server:*",
    "client:dev": "npm --prefix ./client/ run dev",
    "server:dev": "npm --prefix ./server/ run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Zydrunas (https://github.com/ZydrunasK)",
  "license": "ISC",
  "dependencies": {
    "multer": "^1.4.5-lts.1",
    "npm-run-all": "^4.1.5"
  }
}
